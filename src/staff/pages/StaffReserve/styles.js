import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles((theme) => ({
    container: {
        marginTop: '200px ',
    },
    clearIcon: {
        cursor: 'pointer',
    },
    editIcon: {
        cursor: 'pointer',
    },
    rowDone: {
        backgroundColor: '#bdbdbd'
    },
}));